class Product {
    constructor(title, price, imageSource, description, id, userId) {
        this.title = title
        this.price = price
        this.imageSource = imageSource
        this.description = description
        this.id = id
        this.userId = userId
    }
}

export default Product;